merge_protections_settings:
  reporting_method: check-runs
  auto_merge_conditions: true

merge_protections:
  - name: Automatic merge for PRs
    if:
      - base = main
    success_conditions:
      - "check-success=agents / verify (pull_request)"
