1.  **Analyze the failing Optuna trial:** The `optimize.py` script crashes during the meshing phase because Gmsh returns an `Invalid boundary mesh (overlapping facets)` error when trying to generate a mesh for the `violin_body.step` generated by `cad/violin.py` with the specific parameters generated by Optuna in that trial.
2.  **Fix the meshing error handling:** Currently, `optimize.py` calls `subprocess.run(["python3", "mesh/mesher.py"], check=True)`. If `mesher.py` fails, an exception is raised, which bubbles up and crashes the entire Optuna study because `optuna` by default raises the exception when `objective` fails.
3.  **Update `optimize.py` to catch meshing exceptions and return a penalty score:** Optuna can handle failed trials if we catch the exception and raise `optuna.TrialPruned()` or return a high penalty score. The better approach for a crash in CAD/meshing in evolutionary optimization is often to return a heavily penalized score or prune the trial so the study can continue.
4.  **Steps to implement:**
    *   In `opt/optimize.py`, wrap the CAD generation (`cad/violin.py`), Slicing (already wrapped but should fail gracefully), Meshing (`mesh/mesher.py`), and Simulation steps in a `try...except Exception as e:` block.
    *   If an exception occurs during CAD generation, meshing, or simulation, print a warning and raise `optuna.TrialPruned()` to tell Optuna that this trial failed due to invalid geometry and should be ignored, allowing the study to continue.
5.  **Review the final best trial generation:** The final generation in `optimize.py` after the study also needs to handle potential errors gracefully.
6.  **Run pre-commit checks** using `./AGENTS.sh verify`.
